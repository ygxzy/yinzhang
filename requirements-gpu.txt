# GPU 版本依赖（CUDA 12.6）
# 使用：docker build -f Dockerfile.gpu --build-arg TARGET=gpu .
-r requirements-base.txt

# PaddlePaddle GPU 版（需从百度源安装，指定 CUDA 12.6）
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.1.1

# PaddleOCR + PaddleX
paddleocr>=3.6.0
paddlex[ocr]==3.3.13
